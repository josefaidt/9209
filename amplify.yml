version: 1
backend:
  phases:
    preBuild:
      commands:
        - nvm install
        - npm i -g yarn
        - yarn global add @aws-amplify/cli
    build:
      commands:
        - amplifyPush --simple
