version: 1
backend:
  phases:
    preBuild:
      commands:
        - nvm install
        - ./patch
        - yarn global add @aws-amplify/cli
    build:
      commands:
        - amplifyPush --simple
