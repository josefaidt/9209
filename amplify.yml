version: 1
backend:
  phases:
    preBuild:
      commands:
        - nvm install 12
        # - ./patch
        - yarn global add @aws-amplify/cli
    build:
      commands:
        - amplifyPush --simple
frontend:
  phases:
    build:
      commands: []
  artifacts:
    files:
      - '**/*'
    baseDirectory: /public
  cache:
    paths: []
